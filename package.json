{
  "name": "fichacliente",
  "private": true,
  "workspaces": ["frontend", "server", "packages/*"],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "build": "npm run build -w @fichacliente/api-contracts && npm run build -w server && npm run build -w frontend",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
